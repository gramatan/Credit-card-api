apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gran-photo-storage-pvc
spec:
  storageClassName: yc-network-ssd
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
