apiVersion: v1
kind: Service
metadata:
  name: {{ include "gran-cc.fullname" . }}
  labels:
    {{- include "gran-cc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gran-cc.selectorLabels" . | nindent 4 }}
